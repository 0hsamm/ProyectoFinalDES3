import { LlamadaEntranteComponent } from './llamada-entrante';

describe('LlamadaEntranteComponent', () => {
  it('should export the component', () => {
    expect(LlamadaEntranteComponent).toBeDefined();
  });
});
