import { ConversacionService } from './conversacion.service';

describe('ConversacionService', () => {
  it('should export the service class', () => {
    expect(ConversacionService).toBeDefined();
  });
});
